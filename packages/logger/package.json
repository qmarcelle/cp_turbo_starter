
{
  "name": "@repo/logger",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --external winston",
    "lint": "eslint src/",
    "test": "jest",
    "clean": "del-cli dist/**"
  },
  "dependencies": {
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/jest": "^29.5.11",
    "tsup": "^8.0.2",
    "typescript": "^5.4.5",
    "jest": "^29.7.0"
  }
}
